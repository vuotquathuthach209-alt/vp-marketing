# Progress Log — 2026-04-22

> **Trạng thái:** tạm dừng cuối ngày, mai tiếp tục.

## 🎯 Phiên làm việc hôm nay đã hoàn thành

### Phần 1 — Bot Zalo/Facebook routing (sáng)
10/10 scenario khách nhắn trực tiếp đã PASS:

| # | Input | Intent đúng | Status |
|---|-------|-------------|--------|
| 1 | "Giá phòng bao nhiêu" | `generic_price` | ✅ |
| 2 | "Còn phòng trống 25/5 không" | `generic_availability` | ✅ |
| 3 | "Đặt phòng Seehome 2 người tối nay" | `hotel_overview` | ✅ |
| 4 | "Cho mình xem ảnh phòng" | `image_response` | ✅ |
| 5 | "Check-in mấy giờ" | `generic_checkin_time` | ✅ |
| 6 | "0909123456 Nguyễn Văn A" | `contact_captured` | ✅ |
| 7 | "Seehome Airport" (standalone) | `hotel_overview_direct` | ✅ |
| 8 | "Tôi muốn book homestay ngày mai 2 người" | `funnel_budget_ask` | ✅ |
| 9 | "Giá rẻ nhất bao nhiêu" | `generic_price` | ✅ |
| 10 | "Có wifi không" | `generic_wifi` | ✅ |

**Commits:** `72c6112` → `c5cf4d1`

**Fix chính trong `src/services/funnel-dispatcher.ts`:**
- ROUTE 0.5: Bare hotel name (<40 chars, fuzzy match) → hotel_overview_direct
- ROUTE 2.5: Image request move BEFORE clarify + regex fallback (Vietnamese diacritics không `\b`)
- ROUTE 5: `customer_contacts` dùng canonical schema (sender_name, hotel_id NOT NULL)
- `room_images` query: drop `is_primary` column không tồn tại
- Gemini classifier hoạt động đúng cho 9 primary intents

### Phần 2 — Content Intelligence auto-weekly (chiều)
**Vấn đề user báo:**
- `weekly_limit_reached(3/3)` khi bấm "Đăng ngay"
- Muốn bot tự đăng 1 bài/tuần, tự lấy cảm hứng từ web

**Commits:** `1159b0d` → `bcfa464`

**Fix/feature:**
1. `news-publisher.publishNow`: thêm `force=true` default → manual button bypass weekly limit
2. **NEW file:** `src/services/ci-auto-weekly.ts`
   - Cron `0 2 * * 1` UTC = Thứ 2 9h sáng VN
   - Nguồn: `news_articles` (classifier `is_travel_relevant=1`, `political_risk<0.3`)
   - Blacklist keywords: Mỹ:, chính trị, bầu cử, chiến tranh, Trump, Biden, Putin, Xi Jinping...
   - analyzeInspiration → remixPost (transform >50%) → publish FB với og:image
   - 1 bài/tuần/hotel, check `remix_drafts.published_at` trong 7d
   - Telegram notify admin
3. API mới:
   - `POST /content-intel/auto-weekly/run-now?force=1`
   - `GET /content-intel/auto-weekly/status`

**Test passed — live FB post verified:**
- https://www.facebook.com/122126355825105277/posts/122126355699105277
- Remix 693 chars, originality=1.0, fanpage Sonder Apartment Hotel

## 📋 Lịch auto tiếp theo

- **Thứ 2, 27/04/2026, 9h sáng VN time** → bot tự lấy inspiration mới, remix, đăng.
- Scheduler đã đăng ký, không cần thao tác thủ công.

## 🎯 Mai tiếp tục — gợi ý next steps

### A. Content Intelligence enhancements (nếu user muốn thêm)
1. **Thêm nguồn inspiration ngoài news RSS:**
   - Scrape top FB page khách sạn đối thủ (Vinpearl, Mường Thanh, Sun World)
   - Parse Instagram hashtags (#dulichvietnam, #khachsan, #homestay)
   - Web scrape travel blogs (VnExpress Du lịch, Vietnamnet Travel)
2. **Generate AI image** khi article không có og:image (Pollinations gen từ topic_tags)
3. **Multi-post strategy:** thêm 1 "seasonal" post vào dịp lễ (30/4, 1/5, 2/9, Tết)
4. **Admin UI:** xem preview bài sắp auto-post → phê duyệt/chỉnh trước khi đăng (optional)
5. **Analytics:** so sánh engagement bài auto-post vs bài manual

### B. Cleanup / monitoring
1. Hôm nay đã push test post lên Sonder Apartment Hotel fanpage. Check xem bài còn hiển thị không, có bị FB flag không
2. Xem `pm2 logs vp-mkt | grep ci-weekly` sáng thứ 2 để confirm cron chạy đúng giờ
3. Check Telegram notify khi cron chạy

### C. Pending từ session cũ (chưa làm)
- Các improvements khác từ 8-day sales funnel plan (feature flag, metrics, A/B test)
- Retention cleanup test trên production
- Customer memory tier-based personalization verify

## 🔧 Commands cheatsheet

```bash
# SSH vào VPS
ssh root@103.82.193.74
# Password: cCxEvKZ0J3Ee6NJG

# Xem log CI weekly
pm2 logs vp-mkt --lines 100 | grep -i ci-weekly

# Chạy auto weekly ngay (test)
cd C:\Users\USER\tự động đăng facebook
python scripts/test-ci-auto-weekly.py cCxEvKZ0J3Ee6NJG

# Deploy code mới
python scripts/deploy-ssh-full.py cCxEvKZ0J3Ee6NJG

# Test 10 scenario khách nhắn
python scripts/test-direct-questions.py cCxEvKZ0J3Ee6NJG
```

## 📁 Files quan trọng đã edit hôm nay

```
src/services/funnel-dispatcher.ts        (10 scenario routing)
src/services/news-publisher.ts           (force flag)
src/services/ci-auto-weekly.ts           (NEW — CI weekly poster)
src/services/scheduler.ts                (cron đăng ký)
src/routes/content-intel.ts              (API /auto-weekly/*)
scripts/test-ci-auto-weekly.py           (NEW — test script)
scripts/test-direct-questions.py         (10 scenario test)
scripts/debug-route5.py, check-images.py (NEW — debug tools)
```

## 🌲 Last commit

```
bcfa464 fix(ci-weekly): filter out political topics — article selection safer
1159b0d feat(content-intel): auto weekly poster + manual publish bypass limit
c5cf4d1 fix(funnel): room_images query — drop non-existent is_primary column
ae7353a fix(funnel): image regex — drop \b word boundary (broken with Vietnamese diacritics)
882380b feat(funnel): route refinement — image-first, bare hotel name overview
51fb233 fix(funnel): contact_info route — use canonical customer_contacts schema
25e3145 debug(funnel): log Route 5 condition evaluation
72c6112 fix(funnel): dedupe contact_info route — single try block, correct name extraction
```
