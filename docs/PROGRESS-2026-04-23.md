# Progress Log — 2026-04-23

## 🎯 Hôm nay đã làm

### Phần 1 — Kiểm tra sức khoẻ hệ thống (sáng)
- PM2 online, uptime 15h, 163 restarts, RAM 62MB ✅
- Scheduler đăng ký đủ cron (ci-weekly có trong code deploy) ✅
- Bài CI post hôm qua (22/04) vẫn ở DB status=published, FB verify fail do access_token thiếu `pages_read_engagement` (không phải bài bị gỡ) ✅
- News pipeline vẫn chạy: 258 articles filtered + 40 pending + 16 ingested trong 24h ✅
- Telegram 409 spam log → cần fix

### Phần 2 — 5 commit cải tiến
**Commits:** `736a966` → `1624e30`

1. **Calendar week logic** (thay 7-day rolling):
   - `startOfCurrentWeekVN()` = Monday 00:00 VN time
   - `ciPublishedThisWeek()` đếm từ tuần lịch hiện tại → thứ 2 tới reset counter đúng

2. **Social/Blog inspiration fetcher** — user yêu cầu "mạng xã hội hoặc web site":
   - File mới `src/services/social-inspiration-fetcher.ts`
   - 5 RSS sources VN working: VnExpress Du lịch/Kinh doanh, iVIVU, VietnamPlus, Tuổi Trẻ
   - Test thực tế: 50 fetched, 10 saved, 10 analyzed (Gemini phân tích hook/emotion/structure/CTA)
   - FB public pages gated qua `FB_INGEST_ENABLED=1` env (cần App Review permission trước khi bật)

3. **AI image generation** — Pollinations fallback khi article không có og:image:
   - Tạo prompt từ topic_tags của Gemini analysis
   - Prompt pattern: `"Vietnamese travel scene: {tags}, hotel apartment, warm lighting, cinematic, no text, no logo"`
   - Lưu file local + publish `publishImage()` (FB CDN)

4. **Priority 0 source selection**:
   - Ưu tiên `inspiration_posts` (blog/social user-curated) > `news_articles` (RSS chung)
   - Social trend mới, relevant hơn news chung chung

5. **Telegram 409 fix**:
   - Detect HTTP 409 Conflict → `deleteWebhook` + backoff 60s (thay vì 5s spam log)

### Phần 3 — Test end-to-end
- Reset counter + run `runWeeklyAutoPost(1)` force
- Inspiration pick: VnExpress #3 "Khách quốc tế e ngại đến Mỹ xem World Cup 2026"
- Remix: 760 chars, originality=1.0, transform hoàn toàn sang Sonder pitch
  - "Anh/chị từng lo lắng về thủ tục thuê nhà rườm rà hay chi phí phát sinh không lường trước..."
- og:image fetched + published
- **✅ FB post live:** `892083053979896_122126446599105277`
- draft #3 status=published, published_at=2026-04-23T01:43Z

## 🔧 Cấu hình cron

```javascript
// scheduler.ts
cron.schedule('0 2 * * 1', async () => {
  // Thứ 2 9h VN time — ingest social + remix + publish 1 bài
  const { runWeeklyAutoPostAllHotels } = require('./ci-auto-weekly');
  await runWeeklyAutoPostAllHotels();
});
```

## 🏗️ Architecture flow

```
   ┌──────────────────┐     ┌──────────────────┐
   │  RSS VN blogs    │     │  news_articles   │
   │  (5 sources)     │     │  (classifier OK) │
   └────────┬─────────┘     └────────┬─────────┘
            │                        │
            ▼                        ▼
     ┌─────────────────────────────────────┐
     │   inspiration_posts (analyzed)      │
     │   - pattern_hook, emotion, ...      │
     │   - remix_angle_suggestions         │
     └─────────────┬───────────────────────┘
                   │
                   ▼ Priority 0 pick
     ┌─────────────────────────────────────┐
     │   remixPost() — Sonder voice        │
     │   originality >= 0.5, transform     │
     └─────────────┬───────────────────────┘
                   │
                   ▼
     ┌─────────────────────────────────────┐
     │   fetchOgImage() or Pollinations AI │
     └─────────────┬───────────────────────┘
                   │
                   ▼
     ┌─────────────────────────────────────┐
     │   FB Graph API publish              │
     │   remix_drafts.status=published     │
     └─────────────────────────────────────┘
```

## 🎯 Next steps (tuỳ chọn, không bắt buộc)

### A. Advanced features
1. **Admin UI preview** — xem bài sắp auto-post sáng thứ 2, sửa/approve trước
2. **A/B test variants** — gen 2-3 version với temperature khác nhau, pick best
3. **Post performance tracking** — auto update metrics (likes/comments/shares) 24h sau post → feed vào `extractInternalPatterns` để CI tự học
4. **Multi-hotel differentiation** — nếu thêm hotel, mỗi hotel có brand voice riêng

### B. Nguồn cảm hứng thêm (optional)
1. **Instagram public hashtag scrape** — #dulichvietnam, #homestaysaigon (fragile, cần proxy)
2. **Pinterest travel boards** — visual inspiration
3. **TikTok trending travel VN** — cần API third-party
4. **Booking.com / Agoda review data** — if có access

### C. Content strategy
1. **Seasonal calendar** — auto-post trước lễ lớn: 30/4, 1/5, 2/9, Tết, 8/3, 20/10, Giáng Sinh, etc.
2. **User-generated content** — repost hashtag khách tag #sondervn
3. **Behind-the-scenes** — story về hotel operations, check-in tips, district guides

## 📋 Lịch tiếp theo

- **Thứ 2, 27/04/2026, 9h sáng VN** → cron tự chạy `runWeeklyAutoPostAllHotels()`
- Không cần thao tác thủ công
- Telegram sẽ notify admin mỗi lần auto post

## 🆘 Troubleshoot commands

```bash
# SSH
ssh root@103.82.193.74  # password: cCxEvKZ0J3Ee6NJG

# Xem log CI
pm2 logs vp-mkt --lines 100 | grep -iE 'ci-weekly|social-insp'

# Test manual CI weekly (force reset tuần hiện tại)
python scripts/test-ci-v2.py cCxEvKZ0J3Ee6NJG

# Health check
python scripts/daily-health-check.py cCxEvKZ0J3Ee6NJG

# Deploy code mới
python scripts/deploy-ssh-full.py cCxEvKZ0J3Ee6NJG
```

## 🌲 Commits hôm nay

```
1624e30 chore(social-insp): trim broken RSS sources + gate FB ingest by env flag
736a966 feat(ci-weekly): social/blog sources + AI image gen + calendar week
03daf7e docs: progress log 2026-04-22 — end of day checkpoint
```
